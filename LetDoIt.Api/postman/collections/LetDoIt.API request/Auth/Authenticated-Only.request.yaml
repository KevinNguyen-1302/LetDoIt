$kind: http-request
name: Authenticated Only
method: GET
url: '{{baseUrl}}/api/User/AuthenticatedOnlyEndpoint'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
